import { css } from '@emotion/css';

export const friendsListStyle = css({
  width: 250,
});

export const friendsListTitleStyle = css({
  width: '100%',
  textAlign: 'center',
  padding: '10px 0',
  marginBottom: 10,
});
